import 'package:flutter/material.dart';

class ShowDataImc extends StatelessWidget {
  const ShowDataImc({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: const Text("Resultado IMC"),
      ),
    );
  }
}